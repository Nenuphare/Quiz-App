import { StyleSheet } from "react-native";

export default StyleSheet.create({

    container: {
        backgroundColor: '#fff',
        flex: 1,
        alignItems: "center",
        justifyContent: 'center',
    },
    title: {
        flex: 1,
        alignItems: "center",
        justifyContent: 'center',
        fontSize: 25,
        fontWeight: 'bold',
    }
})